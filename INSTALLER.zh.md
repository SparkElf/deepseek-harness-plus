# Plus 桌面安装器

状态：开发中

[English](INSTALLER.md)

## 你要完成的事

你想得到一套本地 Harness 工作环境，不必自己 clone 仓库、记住配置目录，也不必每次打开终端去启动或停止服务。安装器会建立一个隔离的本地 runtime，关掉窗口后仍可通过系统托盘控制它。

## 首版流程

1. 选择一个空的安装目录，以及一个已经存在的工作区目录。
2. 选择 Code Mode、本地端口，以及安装完成后是否立即启动守护进程。
3. 填写 DeepSeek API key、默认模型和可选的推理强度。
4. 安装器 clone SparkElf/deepseek-harness-plus、安装锁定依赖、构建源码、写入隔离的 DSH_HOME，并在你选择后启动本地 Web 服务。
5. 托盘菜单可以打开本地 UI、启动或停止服务、打开日志目录，或退出守护进程。

隔离的 DSH_HOME 位于你选择的安装目录下。这样 Plus runtime 不会覆盖你已有的其他 Harness 安装。API key 只写入本地 runtime 的环境文件；平台支持时会设置为仅当前 owner 可读写。

当前 source-build 安装器要求本机已经安装 Git 和 pnpm。它会在 clone 任何仓库前检查这两个工具，并在安装窗口中报告缺失项。自带工具链会等到发布包能够验证相应的额外管理责任后再考虑。

## 提供方范围

首版安装器只配置原生 deepseek-official 路由，并把选定的默认模型和推理强度写入 Harness settings 文档。自定义 endpoint 或 OpenAI 兼容提供方仍可在安装后通过现有 Web UI 配置；安装器不会把自己无法验证的提供方配置伪装成已经导入。

## UI 约定

安装器沿用当前 Harness 的视觉语言：接近白色的安静表面、紧凑的控件、黑色主操作、灰色结构和克制的蓝色状态强调。第一个窗口是可操作的安装工具，不是落地页。它包含位置、模型和确认三个步骤。每个字段在自己所在位置展示验证结果。开始写入文件前，确认页会展示目标目录、隔离 home、提供方路由、模型、端口和启动行为。

## 验收

- 你可以用空目标目录、工作区目录、DeepSeek key 和模型 id 完成安装。
- 应用会把 fork clone 到该目录，创建隔离 Harness home，写入选定的原生提供方设置，且不修改其他 Harness home。
- 安装完成后，你可以通过托盘启动、停止、打开或退出本地服务。
- clone、依赖安装、构建、写 settings 或启动服务失败时，安装器会在界面中显示失败，且不会声称安装完成。
- 生成的桌面安装包包含这条流程需要的 app、renderer 和 daemon 代码。
