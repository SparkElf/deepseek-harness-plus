diff --git a/packages/session-query/session-query-sqlite/src/index.ts b/packages/session-query/session-query-sqlite/src/index.ts
index 0fd390ee6a..3268c040f4 100644
--- a/packages/session-query/session-query-sqlite/src/index.ts
+++ b/packages/session-query/session-query-sqlite/src/index.ts
@@ -140,7 +140,13 @@ interface ObservedSession {
 interface ObservedPersistedSession {
   header: SessionHeader
   revision: SessionPersistenceRevision
-  loaded?: ObservedSession
+}
+
+interface PersistedObservationSink {
+  beginAttempt(): void
+  write(entry: ObservedPersistedSession, observed: ObservedSession): void
+  commitAttempt(): void
+  discardAttempt(): void
 }
 
 interface PersistenceBinding {
@@ -415,51 +421,70 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
     ).all() as unknown as IndexedLiveRow[]
     const persistedById = new Map(persistedRows.map(row => [row.id as SessionId, row]))
     const liveById = new Map(liveRows.map(row => [row.id as SessionId, row]))
-    const observation = await this._observeStable(persistedById, signal)
-    assertNotAborted(signal)
-    const persistentChanges = observation.persistenceBinding.service === undefined
-      ? []
-      : [...observation.persisted.values()].filter(entry => entry.loaded !== undefined)
-    const persistentDeletes = observation.persistenceBinding.service === undefined
-      ? []
-      : persistedRows.filter(row => !observation.persisted.has(row.id as SessionId))
-    const liveChanges = [...observation.live.values()].filter((entry) => {
-      const indexed = liveById.get(entry.header.id)
-      const persisted = observation.persisted.has(entry.header.id) ? 1 : 0
-      return indexed?.fingerprint !== entry.fingerprint || indexed.persisted !== persisted
-    })
-    const liveDeletes = liveRows.filter(row => !observation.live.has(row.id as SessionId))
-    const pointerChanged = this._lastPersistenceIdentity !== undefined
-      && this._lastPersistenceIdentity !== observation.persistenceBinding.identity
-    const hasWrites = persistentChanges.length > 0
-      || persistentDeletes.length > 0
-      || liveChanges.length > 0
-      || liveDeletes.length > 0
-
-    let nextMainGeneration = this._mainGeneration()
+    const currentMainGeneration = this._mainGeneration()
+    const changedMainGeneration = currentMainGeneration + 1
+    let persistentChanges = 0
     let nextLocalGeneration = this._localGeneration
-    if (persistentChanges.length > 0 || persistentDeletes.length > 0) nextMainGeneration += 1
-    const liveReplacements = liveChanges.map((entry) => {
-      nextLocalGeneration = Math.max(nextLocalGeneration, nextMainGeneration) + 1
-      return {
-        entry,
-        generation: nextLocalGeneration,
-        persisted: observation.persisted.has(entry.header.id),
-      }
-    })
+    let began = false
+    let observation: Observation
+    let pointerChanged = false
+    let hasWrites = false
+    try {
+      db.exec('BEGIN IMMEDIATE')
+      began = true
+      observation = await this._observeStable(persistedById, signal, {
+        beginAttempt: () => {
+          persistentChanges = 0
+          db.exec('SAVEPOINT session_query_observation')
+        },
+        write: (entry, observed) => {
+          try {
+            this._replacePersistedSession(observed, entry.revision, changedMainGeneration)
+            persistentChanges += 1
+          } catch (error: unknown) {
+            throw new SessionQueryError(
+              `session-search reconciliation failed: ${errorMessage(error)}`,
+              'SESSION_QUERY_INDEX_FAILED',
+              { cause: error },
+            )
+          }
+        },
+        commitAttempt: () => { db.exec('RELEASE session_query_observation') },
+        discardAttempt: () => {
+          db.exec('ROLLBACK TO session_query_observation')
+          db.exec('RELEASE session_query_observation')
+        },
+      })
+      assertNotAborted(signal)
+      const indexedAfterObservation = db.prepare(
+        'SELECT id, revision, generation FROM persisted_sessions',
+      ).all() as unknown as IndexedPersistedRow[]
+      const persistentDeletes = observation.persistenceBinding.service === undefined
+        ? []
+        : indexedAfterObservation.filter(row => !observation.persisted.has(row.id as SessionId))
+      const liveChanges = [...observation.live.values()].filter((entry) => {
+        const indexed = liveById.get(entry.header.id)
+        const persisted = observation.persisted.has(entry.header.id) ? 1 : 0
+        return indexed?.fingerprint !== entry.fingerprint || indexed.persisted !== persisted
+      })
+      const liveDeletes = liveRows.filter(row => !observation.live.has(row.id as SessionId))
+      pointerChanged = this._lastPersistenceIdentity !== undefined
+        && this._lastPersistenceIdentity !== observation.persistenceBinding.identity
+      const persistenceChanged = persistentChanges > 0 || persistentDeletes.length > 0
+      const nextMainGeneration = persistenceChanged ? changedMainGeneration : currentMainGeneration
+      const liveReplacements = liveChanges.map((entry) => {
+        nextLocalGeneration = Math.max(nextLocalGeneration, nextMainGeneration) + 1
+        return {
+          entry,
+          generation: nextLocalGeneration,
+          persisted: observation.persisted.has(entry.header.id),
+        }
+      })
+      hasWrites = persistenceChanged || liveChanges.length > 0 || liveDeletes.length > 0
 
-    if (hasWrites) {
-      let began = false
-      try {
-        db.exec('BEGIN IMMEDIATE')
-        began = true
+      if (hasWrites) {
         for (const row of persistentDeletes) this._deleteSession('persisted', row.id as SessionId)
-        for (const entry of persistentChanges) {
-          /* v8 ignore next -- observation loads every entry whose revision differs */
-          if (entry.loaded === undefined) throw new Error(`missing loaded revision for session "${entry.header.id}"`)
-          this._replacePersistedSession(entry.loaded, entry.revision, nextMainGeneration)
-        }
-        if (persistentChanges.length > 0 || persistentDeletes.length > 0) {
+        if (persistenceChanged) {
           db.prepare('UPDATE search_state SET global_generation = ? WHERE singleton = 1').run(nextMainGeneration)
         }
         for (const row of liveDeletes) this._deleteSession('live', row.id as SessionId)
@@ -467,22 +492,26 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
           this._replaceLiveSession(entry, generation, persisted)
         }
         db.exec('COMMIT')
-      } catch (error: unknown) {
-        /* v8 ignore next -- a BEGIN failure has no transaction to roll back; the common wrapper still reports it. */
-        if (began) {
-          /* v8 ignore next 5 -- ROLLBACK failure requires a SQLite double fault; the original failure remains actionable. */
-          try {
-            db.exec('ROLLBACK')
-          } catch {
-            // The original SQLite failure remains the actionable cause.
-          }
+      } else {
+        db.exec('ROLLBACK')
+      }
+      began = false
+    } catch (error: unknown) {
+      /* v8 ignore next -- a BEGIN failure has no transaction to roll back; the common wrapper still reports it. */
+      if (began) {
+        /* v8 ignore next 5 -- ROLLBACK failure requires a SQLite double fault; the original failure remains actionable. */
+        try {
+          db.exec('ROLLBACK')
+        } catch {
+          // The original failure remains the actionable cause.
         }
-        throw new SessionQueryError(
-          `session-search reconciliation failed: ${errorMessage(error)}`,
-          'SESSION_QUERY_INDEX_FAILED',
-          { cause: error },
-        )
       }
+      if (error instanceof SessionQueryError) throw error
+      throw new SessionQueryError(
+        `session-search reconciliation failed: ${errorMessage(error)}`,
+        'SESSION_QUERY_INDEX_FAILED',
+        { cause: error },
+      )
     }
 
     if (hasWrites || pointerChanged) this._globalGeneration += 1
@@ -495,65 +524,74 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
   private async _observeStable(
     indexed: ReadonlyMap<SessionId, IndexedPersistedRow>,
     signal: AbortSignal | undefined,
+    sink: PersistedObservationSink,
   ): Promise<Observation> {
     for (let attempt = 0; attempt < STABLE_OBSERVATION_ATTEMPTS; attempt += 1) {
-      assertNotAborted(signal)
-      const persistenceBinding = this._persistenceBinding
-      const persistence = persistenceBinding.service
-      const initiallyLive = new Set(this.ctx.sessions.list().map(session => session.id))
-      let persisted = new Map<SessionId, ObservedPersistedSession>()
-      if (persistence !== undefined) {
-        try {
-          const canReuseIndexed = this._lastPersistenceIdentity === undefined
-            || this._lastPersistenceIdentity === persistenceBinding.identity
-          const listOptions = signal === undefined ? undefined : { signal }
-          const before = await persistence.list(listOptions)
-          assertNotAborted(signal)
-          persisted = materializePersistenceSnapshots(before)
-          for (const entry of persisted.values()) {
-            if (canReuseIndexed && indexed.get(entry.header.id)?.revision === entry.revision) continue
-            // Skip work already shadowed by a live owner. The cold read is
-            // non-mutating (interrupted turns are balanced in memory only), so
-            // an owner attaching after this check cannot cause side effects;
-            // the live-membership retry below makes the returned observation
-            // live-preferred.
-            if (initiallyLive.has(entry.header.id) || this.ctx.sessions.get(entry.header.id) !== undefined) continue
+      let committed = false
+      sink.beginAttempt()
+      try {
+        assertNotAborted(signal)
+        const persistenceBinding = this._persistenceBinding
+        const persistence = persistenceBinding.service
+        const initiallyLive = new Set(this.ctx.sessions.list().map(session => session.id))
+        let persisted = new Map<SessionId, ObservedPersistedSession>()
+        if (persistence !== undefined) {
+          try {
+            const canReuseIndexed = this._lastPersistenceIdentity === undefined
+              || this._lastPersistenceIdentity === persistenceBinding.identity
+            const listOptions = signal === undefined ? undefined : { signal }
+            const before = await persistence.list(listOptions)
             assertNotAborted(signal)
-            const loaded = await readColdSessionLog(persistence, entry.header.id, signal)
+            persisted = materializePersistenceSnapshots(before)
+            for (const entry of persisted.values()) {
+              if (canReuseIndexed && indexed.get(entry.header.id)?.revision === entry.revision) continue
+              // Skip work already shadowed by a live owner. The cold read is
+              // non-mutating (interrupted turns are balanced in memory only), so
+              // an owner attaching after this check cannot cause side effects;
+              // the live-membership retry below makes the returned observation
+              // live-preferred.
+              if (initiallyLive.has(entry.header.id) || this.ctx.sessions.get(entry.header.id) !== undefined) continue
+              assertNotAborted(signal)
+              const loaded = await readColdSessionLog(persistence, entry.header.id, signal)
+              assertNotAborted(signal)
+              assertSessionHeadersCompatible(entry.header, loaded.header)
+              sink.write(entry, observeSession(loaded.header, loaded.inheritedEventCount, loaded.events))
+            }
             assertNotAborted(signal)
-            assertSessionHeadersCompatible(entry.header, loaded.header)
-            entry.loaded = observeSession(loaded.header, loaded.inheritedEventCount, loaded.events)
-          }
-          assertNotAborted(signal)
-          const afterSnapshots = await persistence.list(listOptions)
-          assertNotAborted(signal)
-          const after = materializePersistenceSnapshots(afterSnapshots)
-          if (!samePersistenceSnapshots(persisted, after)) continue
-          if (this._persistenceBinding !== persistenceBinding) continue
-        } catch (error: unknown) {
-          if (isAbort(error) || signal?.aborted) {
-            throw new SessionQueryError('session-search aborted', 'SESSION_QUERY_ABORTED', {
-              cause: error,
-            })
+            const afterSnapshots = await persistence.list(listOptions)
+            assertNotAborted(signal)
+            const after = materializePersistenceSnapshots(afterSnapshots)
+            if (!samePersistenceSnapshots(persisted, after)) continue
+            if (this._persistenceBinding !== persistenceBinding) continue
+          } catch (error: unknown) {
+            if (isAbort(error) || signal?.aborted) {
+              throw new SessionQueryError('session-search aborted', 'SESSION_QUERY_ABORTED', {
+                cause: error,
+              })
+            }
+            if (this._persistenceBinding !== persistenceBinding) continue
+            if (error instanceof SessionQueryError) throw error
+            throw new SessionQueryError(
+              `session-search persistence observation failed: ${errorMessage(error)}`,
+              'SESSION_QUERY_PERSISTENCE_FAILED',
+              { cause: error },
+            )
           }
-          if (this._persistenceBinding !== persistenceBinding) continue
-          if (error instanceof SessionQueryError) throw error
-          throw new SessionQueryError(
-            `session-search persistence observation failed: ${errorMessage(error)}`,
-            'SESSION_QUERY_PERSISTENCE_FAILED',
-            { cause: error },
-          )
         }
+        const live = new Map<SessionId, ObservedSession>()
+        for (const session of this.ctx.sessions.list()) {
+          const observed = observeLive(session)
+          const durable = persisted.get(session.id)
+          if (durable !== undefined) assertSessionHeadersCompatible(observed.header, durable.header)
+          live.set(session.id, observed)
+        }
+        if (!sameSessionIds(initiallyLive, live)) continue
+        sink.commitAttempt()
+        committed = true
+        return { persistenceBinding, persisted, live }
+      } finally {
+        if (!committed) sink.discardAttempt()
       }
-      const live = new Map<SessionId, ObservedSession>()
-      for (const session of this.ctx.sessions.list()) {
-        const observed = observeLive(session)
-        const durable = persisted.get(session.id)
-        if (durable !== undefined) assertSessionHeadersCompatible(observed.header, durable.header)
-        live.set(session.id, observed)
-      }
-      if (!sameSessionIds(initiallyLive, live)) continue
-      return { persistenceBinding, persisted, live }
     }
     throw new SessionQueryError(
       'session-search persistence observation did not stabilize after one retry',
diff --git a/packages/session/session-format-v0-to-v1/src/codec.ts b/packages/session/session-format-v0-to-v1/src/codec.ts
index 13e09b91c4..cd513ce0e8 100644
--- a/packages/session/session-format-v0-to-v1/src/codec.ts
+++ b/packages/session/session-format-v0-to-v1/src/codec.ts
@@ -1,9 +1,9 @@
 import {
+  assembleSessionFormatArtifact,
   SessionFormatError,
   isSessionFormatJsonObject,
   sessionFormatCount,
   sessionFormatSafeInteger,
-  snapshotSessionFormatArtifact,
   snapshotSessionFormatJson,
 } from '@deepseek-ai/dsh-session-format'
 import type {
@@ -39,11 +39,12 @@ function createReleasedCodec(version: 0 | 1) {
     decodeHeader: (value: unknown) => decodeHeader(value, version),
     decodeArtifact(headerValue: unknown, rowValues: readonly unknown[]) {
       const physical = decodePhysicalHeader(headerValue, version)
-      const artifact = snapshotSessionFormatArtifact({
-        header: physical.header,
-        inheritedEventCount: physical.inheritedEventCount,
-        events: scanRows(rowValues, false).events,
-      }, `released v${version} artifact`)
+      const artifact = assembleSessionFormatArtifact(
+        physical.header,
+        physical.inheritedEventCount,
+        scanRows(rowValues, false).events,
+        `released v${version} artifact`,
+      )
       if (version === 0) assertReleasedV0SourceArtifact(artifact)
       else assertReleasedV1PhysicalArtifact(artifact)
       return artifact
@@ -51,11 +52,12 @@ function createReleasedCodec(version: 0 | 1) {
     decodeRecoverableArtifact(headerValue: unknown, rowValues: readonly unknown[]) {
       const physical = decodePhysicalHeader(headerValue, version)
       const recovered = scanRows(rowValues, true)
-      const artifact = snapshotSessionFormatArtifact({
-        header: physical.header,
-        inheritedEventCount: physical.inheritedEventCount,
-        events: recovered.events,
-      }, `released v${version} recoverable artifact`)
+      const artifact = assembleSessionFormatArtifact(
+        physical.header,
+        physical.inheritedEventCount,
+        recovered.events,
+        `released v${version} recoverable artifact`,
+      )
       if (version === 0) assertReleasedV0SourceArtifact(artifact)
       else assertReleasedV1PhysicalArtifact(artifact)
       return artifact
@@ -197,10 +199,10 @@ function decodeRow(value: SessionFormatJsonValue, rowIndex: number): readonly Se
   if (typeof type === 'string' && PACKED_TAGS.has(type)) return expandPackedRow(record, type, rowIndex)
   if (record['sourceEventSeqs'] !== undefined) {
     const seq = sessionFormatCount(record['seq'], `released Session row ${rowIndex} seq`)
-    return Object.freeze([{
+    return Object.freeze([snapshotSessionFormatJson({
       ...record,
       sourceEventSeqs: decodeSeqRanges(record['sourceEventSeqs'], seq),
-    } as unknown as SessionFormatEvent])
+    }, `released Session row ${rowIndex} provenance`) as SessionFormatEvent])
   }
   return Object.freeze([record as unknown as SessionFormatEvent])
 }
diff --git a/packages/session/session-format-v0-to-v1/src/migration.ts b/packages/session/session-format-v0-to-v1/src/migration.ts
index 21d171f0fc..f0a0b0cb7f 100644
--- a/packages/session/session-format-v0-to-v1/src/migration.ts
+++ b/packages/session/session-format-v0-to-v1/src/migration.ts
@@ -1,9 +1,11 @@
 import {
+  assembleSessionFormatArtifact,
+  isSessionFormatJsonObject,
   SessionFormatError,
   SessionFormatUnsupportedMigrationError,
   defineSessionFormatMigration,
   sessionFormatCount,
-  snapshotSessionFormatArtifact,
+  snapshotSessionFormatJson,
 } from '@deepseek-ai/dsh-session-format'
 import type {
   SessionFormatEvent,
@@ -33,11 +35,12 @@ export const sessionFormatV0ToV1 = defineSessionFormatMigration({
     assertReleasedV0SourceArtifact(source)
     const events = normalizeReleasedV0Events(source.events, source.header.id)
     assertNormalizedReleasedV0Artifact({ ...source, events })
-    const target = snapshotSessionFormatArtifact({
-      header: { ...source.header, version: 1 },
-      inheritedEventCount: source.inheritedEventCount,
+    const target = assembleSessionFormatArtifact(
+      snapshotSessionFormatJson({ ...source.header, version: 1 }, 'released v1 migrated header') as SessionFormatHeader,
+      source.inheritedEventCount,
       events,
-    }, 'released v0-to-v1 target')
+      'released v0-to-v1 target',
+    )
     assertReleasedV1Artifact(target)
     return target
   },
@@ -62,14 +65,58 @@ function normalizeReleasedV0Events(
     const header = normalizeLegacyRequestHeader(end, sessionId)
     const steering = normalizeLegacySteering(header, sessionId)
     const message = normalizeLegacyMessage(steering, sessionId, messageIds)
-    assertReleasedEventPayload(message, 0)
-    output.push(message)
-    const messageId = eventMessageId(message)
+    const descriptor = normalizeLegacySubagentDescriptor(message)
+    const replay = normalizeLegacyReplayState(descriptor)
+    assertReleasedEventPayload(replay, 0)
+    const owned = snapshotSessionFormatJson(replay, `${replay.type} ${replay.seq} normalized`) as SessionFormatEvent
+    output.push(owned)
+    const messageId = eventMessageId(owned)
     if (messageId !== undefined) messageIds.set(message.seq, messageId)
   }
   return Object.freeze(output)
 }
 
+function normalizeLegacySubagentDescriptor(event: SessionFormatEvent): SessionFormatEvent {
+  if (event.type !== 'subagent/descriptor') return event
+  const data = releasedV0Record(event.data, `subagent/descriptor ${event.seq} data`)
+  if (data['version'] !== 2) return event
+  return { ...event, data: { ...data, version: 3 } }
+}
+
+function normalizeLegacyReplayState(event: SessionFormatEvent): SessionFormatEvent {
+  const data = releasedV0Record(event.data, `${event.type} ${event.seq} data`)
+  if (event.type === 'assistant/chunk') {
+    const chunk = releasedV0Record(data['chunk'], `assistant/chunk ${event.seq} chunk`)
+    const replayState = legacyPiAiReplayEnvelope(chunk['replayState'])
+    if (replayState === undefined) return event
+    return { ...event, data: { ...data, chunk: { ...chunk, replayState } } }
+  }
+  if (event.type === 'assistant/message') {
+    const message = releasedV0Record(data['message'], `assistant/message ${event.seq} message`)
+    const source = releasedV0Record(message['source'], `assistant/message ${event.seq} source`)
+    const replayState = legacyPiAiReplayEnvelope(source['replayState'])
+    if (replayState === undefined) return event
+    return {
+      ...event,
+      data: { ...data, message: { ...message, source: { ...source, replayState } } },
+    }
+  }
+  return event
+}
+
+function legacyPiAiReplayEnvelope(value: SessionFormatJsonValue | undefined): SessionFormatJsonValue | undefined {
+  if (!isSessionFormatJsonObject(value)
+    || value['kind'] !== 'pi-ai' || value['version'] !== 1 || !Array.isArray(value['blocks'])) return undefined
+  assertReleasedV0Keys(
+    value,
+    ['kind', 'version', 'api', 'provider', 'model', 'stopReason', 'blocks'],
+    ['responseModel', 'responseId'],
+    'legacy pi-ai replay state',
+  )
+  const { blocks, ...response } = value
+  return { response: { ...response, version: 2 }, blocks }
+}
+
 function normalizeLegacyRequestHeader(event: SessionFormatEvent, sessionId: string): SessionFormatEvent {
   if (event.type !== 'request/header') return event
   const data = releasedV0Record(event.data, `request/header ${event.seq} data`)
diff --git a/packages/session/session-format-v0-to-v1/tests/migration.spec.ts b/packages/session/session-format-v0-to-v1/tests/migration.spec.ts
index 33bb1376e6..42f07cbc25 100644
--- a/packages/session/session-format-v0-to-v1/tests/migration.spec.ts
+++ b/packages/session/session-format-v0-to-v1/tests/migration.spec.ts
@@ -128,6 +128,67 @@ describe('released Session format v0 to v1', () => {
     expect(releasedV1SessionFormatCodec.decodeArtifact(v1, [prefix, event]).events).toEqual([prefix, event])
   })
 
+  it('wraps the pre-envelope pi-ai replay state written by historical v0 sessions', () => {
+    const legacyReplay = {
+      kind: 'pi-ai', version: 1, api: 'openai-responses', provider: 'gateway', model: 'model',
+      responseId: 'response', stopReason: 'stop', blocks: [{ type: 'text', textSignature: 'signature' }],
+    }
+    const header = { type: 'session', version: 0, id: 'legacy-replay', createdAt: 1, delegationDepth: 0 }
+    const rows = [
+      { type: 'turn/start', seq: 0, time: 1, data: { turn: 1 } },
+      { type: 'step/start', seq: 1, time: 2, data: { turn: 1, step: 1 } },
+      {
+        type: 'assistant/chunk', seq: 2, time: 3,
+        data: { turn: 1, step: 1, chunk: { type: 'text-delta', index: 0, text: 'answer' } },
+      },
+      {
+        type: 'assistant/chunk', seq: 3, time: 4,
+        data: { turn: 1, step: 1, chunk: { type: 'finish', reason: { kind: 'stop' }, replayState: legacyReplay } },
+      },
+      {
+        type: 'assistant/message', seq: 4, time: 5, sourceEventSeqs: [2, 3], surfaceOp: 'append',
+        data: {
+          turn: 1, step: 1,
+          message: {
+            id: 'assistant', role: 'assistant', content: [{ type: 'text', text: 'answer' }],
+            source: { kind: 'model', provider: 'gateway', model: 'model', replayState: legacyReplay },
+          },
+        },
+      },
+    ]
+
+    const migrated = sessionFormatV0ToV1.migrate(releasedV0SessionFormatCodec.decodeArtifact(header, rows))
+    const envelope = {
+      response: {
+        kind: 'pi-ai', version: 2, api: 'openai-responses', provider: 'gateway', model: 'model',
+        responseId: 'response', stopReason: 'stop',
+      },
+      blocks: [{ type: 'text', textSignature: 'signature' }],
+    }
+
+    expect(migrated.events[3]?.data).toMatchObject({ chunk: { replayState: envelope } })
+    expect(migrated.events[4]?.data).toMatchObject({ message: { source: { replayState: envelope } } })
+    sessionFormatV0ToV1.validateTarget(migrated)
+  })
+
+  it('promotes descriptor v2 after v3 added only optional reasoning effort', () => {
+    const header = { type: 'session', version: 0, id: 'legacy-descriptor', createdAt: 1, delegationDepth: 0 }
+    const descriptor = {
+      type: 'subagent/descriptor', seq: 0, time: 1,
+      data: {
+        mode: 'continuable', version: 2, provider: 'in-process', label: 'child',
+        agentProvider: 'gateway', agentModel: 'model', persona: 'reviewer', toolFilter: { allow: ['read'] },
+      },
+    }
+
+    const migrated = sessionFormatV0ToV1.migrate(
+      releasedV0SessionFormatCodec.decodeArtifact(header, [descriptor]),
+    )
+
+    expect(migrated.events[0]?.data).toEqual({ ...descriptor.data, version: 3 })
+    sessionFormatV0ToV1.validateTarget(migrated)
+  })
+
   it('preserves a complete canonical multi-owner log except for header.version', () => {
     const physicalHeader = {
       type: 'session', version: 0, id: 'full-identity', createdAt: 1, cwd: '/work', delegationDepth: 0,
diff --git a/packages/session/session-format-v1-to-v2/src/codec.ts b/packages/session/session-format-v1-to-v2/src/codec.ts
index 6708f8e451..9b8c3c6988 100644
--- a/packages/session/session-format-v1-to-v2/src/codec.ts
+++ b/packages/session/session-format-v1-to-v2/src/codec.ts
@@ -1,7 +1,7 @@
 import {
+  assembleSessionFormatArtifact,
   SessionFormatError,
   sessionFormatCount,
-  snapshotSessionFormatArtifact,
   snapshotSessionFormatJson,
 } from '@deepseek-ai/dsh-session-format'
 import type {
@@ -106,7 +106,12 @@ function decodeArtifact(
     events.push(event)
   }
   const inheritedEventCount = deriveInheritedEventCount(header, events)
-  const artifact = snapshotSessionFormatArtifact({ header, inheritedEventCount, events }, 'released v2 artifact')
+  const artifact = assembleSessionFormatArtifact(
+    header,
+    inheritedEventCount,
+    Object.freeze(events),
+    'released v2 artifact',
+  )
   assertReleasedV2PhysicalArtifact(artifact)
   return artifact
 }
diff --git a/packages/session/session-format-v1-to-v2/src/migration.ts b/packages/session/session-format-v1-to-v2/src/migration.ts
index c0e0f651a1..97e2ae3d76 100644
--- a/packages/session/session-format-v1-to-v2/src/migration.ts
+++ b/packages/session/session-format-v1-to-v2/src/migration.ts
@@ -1,12 +1,14 @@
 import { AssistantStreamAccumulator } from '@deepseek-ai/dsh-llm'
 import {
+  assembleSessionFormatArtifact,
   SessionFormatUnsupportedMigrationError,
   defineSessionFormatMigration,
-  snapshotSessionFormatArtifact,
+  snapshotSessionFormatJson,
 } from '@deepseek-ai/dsh-session-format'
 import type {
   SessionFormatArtifact,
   SessionFormatEvent,
+  SessionFormatHeader,
   SessionFormatJsonObject,
   SessionFormatJsonValue,
 } from '@deepseek-ai/dsh-session-format'
@@ -98,11 +100,16 @@ export const sessionFormatV1ToV2 = defineSessionFormatMigration({
       for (const chunk of group.chunks) oldToNew.delete(chunk.seq)
     }
 
-    const target = snapshotSessionFormatArtifact({
-      header: { ...source.header, version: 2 },
+    const events = Object.freeze(staged.map(({ event }, seq) => snapshotSessionFormatJson(
+      remapReferences(event, seq, oldToNew),
+      `released v2 migrated event ${seq}`,
+    ) as SessionFormatEvent))
+    const target = assembleSessionFormatArtifact(
+      snapshotSessionFormatJson({ ...source.header, version: 2 }, 'released v2 migrated header') as SessionFormatHeader,
       inheritedEventCount,
-      events: staged.map(({ event }, seq) => remapReferences(event, seq, oldToNew)),
-    }, 'released v1-to-v2 target')
+      events,
+      'released v1-to-v2 target',
+    )
     assertReleasedV2Artifact(target)
     return target
   },
diff --git a/packages/session/session-format/src/json.ts b/packages/session/session-format/src/json.ts
index b4d7cbba86..a618079a59 100644
--- a/packages/session/session-format/src/json.ts
+++ b/packages/session/session-format/src/json.ts
@@ -2,11 +2,15 @@ import { deepFreeze, snapshotJsonValue } from '@deepseek-ai/dsh-util-values'
 import { SessionFormatError } from './error.ts'
 import type {
   SessionFormatArtifact,
+  SessionFormatEvent,
   SessionFormatHeader,
   SessionFormatJsonObject,
   SessionFormatJsonValue,
 } from './types.ts'
 
+const ownedJsonSnapshots = new WeakSet<object>()
+const ownedArtifacts = new WeakSet<object>()
+
 /**
  * Test whether a value is a non-null, non-array object.
  * @param value - candidate value.
@@ -71,11 +75,16 @@ export function inspectSessionFormatVersion(headerValue: unknown): number {
  * @returns an immutable detached JSON snapshot.
  */
 export function snapshotSessionFormatJson(value: unknown, label = 'Session value'): SessionFormatJsonValue {
+  if (typeof value === 'object' && value !== null && ownedJsonSnapshots.has(value)) {
+    return value as SessionFormatJsonValue
+  }
   const snapshot = snapshotJsonValue(value)
   if (snapshot === undefined) {
     throw new SessionFormatError(`${label} is not lossless JSON`)
   }
-  return deepFreeze(snapshot) as SessionFormatJsonValue
+  const frozen = deepFreeze(snapshot) as SessionFormatJsonValue
+  if (typeof frozen === 'object' && frozen !== null) ownedJsonSnapshots.add(frozen)
+  return frozen
 }
 
 /**
@@ -88,7 +97,48 @@ export function snapshotSessionFormatArtifact(
   artifact: SessionFormatArtifact,
   label = 'Session artifact',
 ): SessionFormatArtifact {
+  if (ownedArtifacts.has(artifact)) return artifact
   const snapshot = snapshotSessionFormatJson(artifact, label) as SessionFormatJsonObject
+  const validated = validateSessionFormatArtifact(snapshot, label)
+  ownedArtifacts.add(validated)
+  return validated
+}
+
+/**
+ * Assemble an artifact from JSON snapshots already detached and frozen by this
+ * module. Codecs use this after snapshotting each physical row, avoiding a
+ * second whole-log traversal without weakening borrowed-input validation.
+ * @param header - owned logical header snapshot.
+ * @param inheritedEventCount - validated inherited prefix length.
+ * @param events - frozen array whose members are owned event snapshots.
+ * @param label - diagnostic subject.
+ * @returns an immutable artifact recognized by subsequent snapshot boundaries.
+ */
+export function assembleSessionFormatArtifact(
+  header: SessionFormatHeader,
+  inheritedEventCount: number,
+  events: readonly SessionFormatEvent[],
+  label = 'Session artifact',
+): SessionFormatArtifact {
+  if (!ownedJsonSnapshots.has(header)) throw new SessionFormatError(`${label} header is not an owned snapshot`)
+  if (!Object.isFrozen(events)) throw new SessionFormatError(`${label} events must be frozen`)
+  for (let index = 0; index < events.length; index += 1) {
+    const event = events[index]
+    if (event === undefined || !ownedJsonSnapshots.has(event)) {
+      throw new SessionFormatError(`${label} event ${index} is not an owned snapshot`)
+    }
+  }
+  const artifact = Object.freeze({ header, inheritedEventCount, events }) as SessionFormatArtifact
+  const validated = validateSessionFormatArtifact(artifact as unknown as SessionFormatJsonObject, label)
+  ownedJsonSnapshots.add(validated)
+  ownedArtifacts.add(validated)
+  return validated
+}
+
+function validateSessionFormatArtifact(
+  snapshot: SessionFormatJsonObject,
+  label: string,
+): SessionFormatArtifact {
   const header = snapshot['header']
   const inheritedEventCount = snapshot['inheritedEventCount']
   const events = snapshot['events']
diff --git a/packages/session/session-format/tests/json.spec.ts b/packages/session/session-format/tests/json.spec.ts
index 0a7bafe5a8..669de96f5e 100644
--- a/packages/session/session-format/tests/json.spec.ts
+++ b/packages/session/session-format/tests/json.spec.ts
@@ -1,5 +1,6 @@
 import { describe, expect, it } from 'vitest'
 import {
+  assembleSessionFormatArtifact,
   inspectSessionFormatVersion,
   sessionFormatCount,
   sessionFormatSafeInteger,
@@ -49,6 +50,26 @@ describe('lossless Session format JSON snapshots', () => {
     expect(() => snapshotSessionFormatJson(new ArrayValue(1))).toThrow(/not lossless JSON/)
   })
 
+  it('reuses owned snapshots and assembles only owned event members', () => {
+    const header = snapshotSessionFormatJson({
+      version: 1, id: 'owned', createdAt: 1, isSeeded: false, delegationDepth: 0,
+    })
+    const event = snapshotSessionFormatJson({ type: 'event', seq: 0, time: 1, data: { value: true } })
+    const artifact = assembleSessionFormatArtifact(
+      header as never,
+      0,
+      Object.freeze([event]) as never,
+    )
+
+    expect(snapshotSessionFormatJson(event)).toBe(event)
+    expect(snapshotSessionFormatArtifact(artifact)).toBe(artifact)
+    expect(() => assembleSessionFormatArtifact(
+      header as never,
+      0,
+      Object.freeze([{ type: 'event', seq: 0, time: 1, data: {} }]) as never,
+    )).toThrow(/not an owned snapshot/)
+  })
+
   it.each([
     ['non-object header', { header: null, inheritedEventCount: 0, events: [] }],
     ['non-array events', { header: { version: 1 }, inheritedEventCount: 0, events: null }],
diff --git a/packages/session/session-persistence-jsonl/src/generation.ts b/packages/session/session-persistence-jsonl/src/generation.ts
index 66b2c53959..2609a91904 100644
--- a/packages/session/session-persistence-jsonl/src/generation.ts
+++ b/packages/session/session-persistence-jsonl/src/generation.ts
@@ -19,13 +19,13 @@ import {
   type FileHandle,
 } from 'node:fs/promises'
 import { basename, dirname, join } from 'node:path'
+import { scheduler } from 'node:timers/promises'
 import type { JsonlCompression } from './format.ts'
 import { generationLogFilename, logSuffix } from './format.ts'
 import { publishNewFileWin32 } from './win32.ts'
 import {
   compressZstdFrame,
   createZstdFrameDecoder,
-  decompressZstdFrame,
   decompressZstdPrefix,
   scanZstdFrames,
 } from './zstd.ts'
@@ -167,6 +167,9 @@ interface DecodedPhysicalJsonl {
   readonly torn: boolean
 }
 
+const ZSTD_DECODE_BATCH_BYTES = 8 * 1024 * 1024
+const ZSTD_DECODE_YIELD_FRAMES = 1024
+
 interface GenerationFileSystem {
   open(path: string, flags: string, mode?: number): Promise<FileHandle>
   readFile(path: string, signal?: AbortSignal): Promise<Buffer>
@@ -349,13 +352,41 @@ async function decodeZstdJsonl(bytes: Buffer, signal?: AbortSignal): Promise<Dec
   /* v8 ignore next -- the independent header probe already established the first frame. */
   if (frames.length === 0) throw new Error('empty or header-less Zstandard session log')
   const complete: Buffer[] = []
-  for (const [index, frame] of frames.entries()) {
-    signal?.throwIfAborted()
-    const plaintext = await decompressZstdFrame(bytes.subarray(frame.start, frame.end))
-    if (index === 0) assertIndependentHeaderFrame(plaintext)
-    complete.push(plaintext)
+  let batch: Buffer[] = []
+  let batchBytes = 0
+  let completeBytesLength = 0
+  const flushBatch = () => {
+    if (batch.length === 0) return
+    complete.push(batch.length === 1 ? batch[0] as Buffer : Buffer.concat(batch, batchBytes))
+    completeBytesLength += batchBytes
+    batch = []
+    batchBytes = 0
+  }
+  const decoder = createZstdFrameDecoder()
+  const decodedFrames = decoder.decode(bytes, frames)
+  try {
+    let index = 0
+    for (const plaintext of decodedFrames) {
+      signal?.throwIfAborted()
+      if (index === 0) assertIndependentHeaderFrame(plaintext)
+      const owned = Buffer.from(plaintext)
+      batch.push(owned)
+      batchBytes += owned.length
+      if (batchBytes >= ZSTD_DECODE_BATCH_BYTES) flushBatch()
+      index += 1
+      if (index % ZSTD_DECODE_YIELD_FRAMES === 0) {
+        await scheduler.yield()
+        signal?.throwIfAborted()
+      }
+    }
+  } finally {
+    decodedFrames.return()
+    decoder.close()
   }
-  const completeBytes = Buffer.concat(complete)
+  flushBatch()
+  const completeBytes = complete.length === 1
+    ? complete[0] as Buffer
+    : Buffer.concat(complete, completeBytesLength)
   if (completeBytes.at(-1) !== 0x0A) {
     throw new Error('corrupt Zstandard session log: complete frame contains a torn JSONL record')
   }
diff --git a/packages/session/session-persistence-jsonl/tests/generation.spec.ts b/packages/session/session-persistence-jsonl/tests/generation.spec.ts
index 16e2f526a6..e975bc99f5 100644
--- a/packages/session/session-persistence-jsonl/tests/generation.spec.ts
+++ b/packages/session/session-persistence-jsonl/tests/generation.spec.ts
@@ -424,6 +424,23 @@ describe('JSONL immutable generation publication', () => {
     expect(await decodeZstdJsonl(tornRequest.currentPath)).toBe(line(header(1)) + line(event0) + line(event1))
   })
 
+  it('migrates a historical generation composed of many small Zstandard frames', async () => {
+    const root = await tempRoot()
+    const request = options(root, 'zstd')
+    const rows = Array.from({ length: 2050 }, (_, seq) => ({ ...event0, seq }))
+    const frames = await Promise.all([
+      compressZstdFrame(line(header(0))),
+      ...rows.map(row => compressZstdFrame(line(row))),
+    ])
+    await writeFile(request.sourcePath, Buffer.concat(frames))
+
+    await ensureJsonlGenerationCurrent(request)
+
+    expect(await decodeZstdJsonl(request.currentPath)).toBe(
+      line(header(1)) + rows.map(row => line(row)).join(''),
+    )
+  })
+
   it('rejects header-less raw and Zstandard sources and a non-independent Zstandard header frame', async () => {
     const root = await tempRoot()
     const rawRoot = join(root, 'raw')
