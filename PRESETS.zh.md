# 社区预设章程

[English](PRESETS.md)

DeepSeek Harness Plus 为可重复的运行场景发布可运行预设。预设是带配套文档的版本化配置，不是在会话中粘贴的一段提示词。

## 发布要求

每个预设发布都定义目标用户、模型与工具权限、所需凭据、持久化 owner、安装命令和验证路径。涉及外部系统的预设还必须定义审批点和可见审计记录。

## 代码交付

代码交付预设面向仓库探索、实现、审查、验证和发布准备。它建立在 Harness Code Mode 之上，并将源码变更、工具使用和验证证据保留在会话记录中。

## 社区运营

社区运营预设支持 Issue 分诊、Discussion 协调、发布说明准备和贡献者协作。没有明确批准的动作时，它不会代表维护者发布内容、关闭问题、添加标签或合并代码。

## 多用户运行时

多用户运行时预设定义共享 Web UI、认证网关，以及每位用户或工作区一个隔离 Harness runtime 的部署。网关负责用户认证和 runtime 路由；runtime 容器负责会话数据、工具、工作区和模型凭据。

## 智能问数

智能问数预设服务于需要基于获准业务数据完成分析的用户。它要求明确数据源 owner、只读或经过显式批准的写权限、语义指标定义、执行前查询审查和可审计的结果记录。它不把数据库凭据或无限制 SQL 视为模型能力。

## 贡献路径

新增运行模型前，请先通过 Discussion 提案。Pull Request 需要同时提交可运行配置、配套文档和验证证据。
