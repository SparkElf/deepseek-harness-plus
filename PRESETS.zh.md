# 规划中的预设

[English](PRESETS.md)

> 状态：目前没有社区预设已经实现或可安装。

本页是预设目录的发布约定。一个能力只有具备可运行配置、明确权限、安装路径和验证证据后，才会在这里被描述为可用。

## 代码交付

**状态：暂未实现。**

面向仓库探索、实现、审查、验证和发布准备的工作流。

## 社区运营

**状态：暂未实现。**

面向 Issue 分诊、Discussion 协调、发布说明准备和贡献者协作的工作流。代表维护者发布内容、关闭问题、添加标签或合并代码前，必须存在明确批准的动作。

## 多用户运行时

**状态：暂未实现。**

由共享 Web UI、认证网关和每位用户或工作区一个隔离 Harness runtime 组成的部署。网关负责认证和路由；runtime 负责会话、工具、工作区和模型凭据。

## 智能问数

**状态：暂未实现。**

基于获准业务数据的受控分析工作流。它要求明确数据源 owner、只读或经过显式批准的写权限、语义指标定义、执行前查询审查和可审计结果记录。

## 发布规则

一个预设发布必须在同一个 Pull Request 中包含配置、权限模型、所需凭据、安装命令和验证说明。
